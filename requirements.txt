edge-tts>=7.2.8
moviepy>=2.0.0
Pillow>=10.0.0
numpy

# Optional: only needed for generate_takes.py SDK backend (CI).
# Local dev uses the `claude` CLI instead and doesn't need this.
anthropic>=0.40.0
